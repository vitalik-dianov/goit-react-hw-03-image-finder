import styled from 'styled-components';

export const SearchButton = styled.button`
  width: 120px;
  height: 40px;
  border: none;
  border-radius: 3px;
  text-transform: uppercase;
  font-weight: 700;

`;
